[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cryptocore"
version = "3.0.0"
description = "Учебная CLI-утилита AES-128 с системным CSPRNG для ключей и IV."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pycryptodome>=3.20,<4",
]

[project.scripts]
cryptocore = "cryptocore.main:main"

[tool.setuptools.packages.find]
where = ["src"]
